# -*- coding: utf-8 -*-
#
# Customer Churn Analysis documentation build configuration file, created by
# sphinx-quickstart.
#
# This file is execfile()d with the current directory set to its containing dir.
#
# Note that not all possible configuration values are present in this
# autogenerated file.
#
# All configuration values have a default; values that are commented out
# serve to show the default.

import sys
import os
from pathlib import Path

# Add the src directory to the Python path
sys.path.append(str(Path(__file__).resolve().parents[1]))

# -- General configuration -----------------------------------------------------

# If your documentation needs a minimal Sphinx version, state it here.
# needs_sphinx = '1.0'

# Add any Sphinx extension module names here, as strings. They can be extensions
# coming with Sphinx (named 'sphinx.ext.*') or your custom ones.

extensions = [
    'sphinx.ext.autodoc',            # Generates documentation from docstrings        # Generates summary pages for modules
    'sphinx.ext.napoleon',           # For Google/NumPy style docstrings      # Optional for type hints
    'sphinx.ext.viewcode',           # Include links to source code        # Enable publishing to GitHub pages
]

# Autodoc options
autodoc_default_options = {
    'members': True,                # Document all members
    'undoc-members': True,           # Document members without docstrings
    'private-members': True,         # Include private members (_private)
    'special-members': '__init__',   # Include special members like __init__
    'show-inheritance': True,        # Show class inheritance
}

# Autosummary settings
autosummary_generate = True          # Automatically generate .rst files for modules
autosummary_imported_members = True  # Include imported members

# Add any paths that contain templates here, relative to this directory.
templates_path = ['_templates']

# The suffix of source filenames.
source_suffix = '.rst'

# The encoding of source files.
# source_encoding = 'utf-8-sig'

# The master toctree document.
master_doc = 'index'

# General information about the project.
project = u'Customer Churn Analysis'

# The version info for the project you're documenting, acts as replacement for
# |version| and |release|, also used in various other places throughout the
# built documents.
#
# The short X.Y version.
version = '0.1'
# The full version, including alpha/beta/rc tags.
release = '0.1'

# List of patterns, relative to source directory, that match files and
# directories to ignore when looking for source files.
exclude_patterns = ['_build']

# The name of the Pygments (syntax highlighting) style to use.
pygments_style = 'sphinx'

# -- Options for HTML output ---------------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
html_theme = 'furo'

# HTML theme options for dark mode
html_theme_options = {
    "dark_mode": True,
}

# Custom static files (like custom.css) need to be in _static directory
html_static_path = ['_static']

# Custom CSS to style the documentation
html_css_files = [
    'custom.css',
]

def setup(app):
    app.add_css_file('custom.css')

# -- Extension configuration ---------------------------------------------------




# -- Options for other output formats ------------------------------------------

# LaTeX, man pages, Texinfo, etc., remain unchanged
latex_elements = {}
latex_documents = [
    ('index', 'customer_churn_analysis.tex', u'Customer Churn Analysis Documentation', u'deep_sutariya', 'manual'),
]
man_pages = [('index', 'customer_churn_analysis', u'Customer Churn Analysis Documentation', [u'deep_sutariya'], 1)]
texinfo_documents = [('index', 'customer_churn_analysis', u'Customer Churn Analysis Documentation', u'deep_sutariya', 'Customer Churn Analysis', 'It is based on telecom customer churn', 'Miscellaneous')]
